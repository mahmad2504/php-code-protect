#include <iostream>
#include <string>
#include <string.h>

using namespace std;
#include "prog.h"


int main(int argc, char **argv)
{
	//char *ndata = (char *) calloc(1,strlen(data.c_str()));
	//string ndata;
	//int i=0;
	//for(i=0;i<strlen(data.c_str());i++)
	//{
	//	data[i] = decode(data.c_str()[i]);
		//putc(ndata[i],stdout);
	//}
	//ndata[i] = '\0';
	//string f =  header+"\""+data+"\""+tail;
    //system(f.c_str());
	Run_prog();
}

